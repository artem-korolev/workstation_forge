#!/bin/bash
# Dracut module for battery boot guard

check() {
  return 0
}

depends() {
  echo "systemd"
  return 0
}

install() {
  inst_hook pre-mount 50 "$moddir/battery-boot-guard.sh"
  inst_multiple cat sleep poweroff reboot
  inst_multiple -o plymouth
}
